#!/usr/bin/env bash
# POSIX wrapper for the Tree Decompose Development Engine.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python "$SCRIPT_DIR/tree-decompose.py" "$@"
